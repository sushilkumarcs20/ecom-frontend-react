import React from 'react'
import Base from '../core/Base'

const UserDashboard = () => {
    return (
        <Base title="User Dashboard" description="Dashboard area of store's User">
            <h1>Welcome to user dashboard</h1>
        </Base>
    )
}

export default UserDashboard
